# Fee Engine environment variables
# Copy this file to .env and fill in real values

PORT=8001
PYTHON_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mapansetu_fees
DB_USER=postgres
DB_PASSWORD=changeme

# IAM Service (for JWT verification)
IAM_SERVICE_URL=http://localhost:8080
